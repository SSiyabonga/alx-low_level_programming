#include "main.h"
#include <stdio.h>

/**
* main - the function that prints the arguments passed to it.
* @argc: the arguments count.
* @argv: the vector array of arguments.
*
* Return: 0
*/

int main(int argc, char *argv[])
{
	(void)argv;
	printf("%d\n", argc);
	return (0);
}
